version: '3'
services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile-backend
    ports:
      - 8080:8080
      - 11337:113371
  frontend:
    build: 
      context: .
      dockerfile: Dockerfile-frontend
    ports:
      - 30001:300011
